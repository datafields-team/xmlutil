from setuptools import setup

version = '1.0.0'

package_dir = 'xmlutil'

setup(name='xmlutil',

    version=version,

    description='a small tool for extract multiple xml file to a table.',

    packages=[package_dir, ],
)
